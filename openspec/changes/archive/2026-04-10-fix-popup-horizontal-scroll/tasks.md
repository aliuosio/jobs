## 1. CSS Fix

- [x] 1.1 Remove `min-width: 760px` from `.popup-container` in `extension/popup/popup.css`

## 2. Verification

- [x] 2.1 Load extension in Firefox and verify popup displays without horizontal scroll
- [x] 2.2 Check that all popup elements (tabs, job links, buttons) are visible and functional
- [x] 2.3 Run any existing tests to ensure no regressions
