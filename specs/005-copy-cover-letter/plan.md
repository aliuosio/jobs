# Implementation Plan: Copy Cover Letter to Clipboard

**Branch**: `005-copy-cover-letter` | **Date**: 2026-04-18 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `specs/005-copy-cover-letter/spec.md`

**Note**: This implementation is complete. All functionality exists in the codebase - only configuration fixes were needed for the n8n workflow.

## Summary

Feature: Copy Cover Letter to Clipboard
Primary requirement: Add copy icon to job links after cover letter generation, allowing one-click copy to clipboard
Technical approach: Reuse existing popup.js code for copy functionality, fix n8n webhook configuration

## Technical Context

**Language/Version**: JavaScript (ES Modules), Node.js for tests  
**Primary Dependencies**: Browser extension APIs, n8n webhook, Clipboard API  
**Storage**: Browser storage.local (extension for job link status), PostgreSQL (job_applications table)  
**Testing**: Node.js unit tests in `extension/tests/`  
**Target Platform**: Firefox browser (extension)  
**Project Type**: Browser extension (Job Links Manager feature)  
**Performance Goals**: N/A - user interface interaction  
**Constraints**: Must work offline with cached data, polling timeout 3 min  
**Scale/Scope**: Individual user extension, typically <100 job listings

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Gate | Status | Notes |
|------|--------|-------|
| I. SOLID/DRY | ✅ PASS | No new code - reusing existing popup.js functions |
| II. Design Patterns | ✅ PASS | Uses existing state machine pattern for cl_status |
| III. Git Flow | ✅ PASS | Already on feature branch |
| IV. TDD | ✅ PASS | Tests exist in `extension/tests/cover-letter.test.js` |
| V. n8n Workflow | ✅ FIXED | Added httpMethod: POST, fixed body parsing |

**Constitution Note**: This is primarily a configuration fix (workflow activation), not new code. The existing extension tests verify the expected behavior. The issues were:
1. n8n webhook missing `httpMethod: 'POST'` parameter
2. CaptureQueryParam reading from `query.job_offers_id` instead of `body.job_offers_id`
3. Unmapped `created_at` dateTime field in Save node schema

## Project Structure

### Documentation (this feature)

```
specs/005-copy-cover-letter/
├── plan.md              # This file
├── research.md          # (not needed - no unknowns)
├── data-model.md        # (not needed - reusing existing entities)
├── quickstart.md        # (exists in feature folder)
└── tasks.md           # Generated by /speckit.tasks
```

### Source Code (repository root)

```
extension/
├── popup/
│   └── popup.js           # Has existing copy/generate code
├── services/
│   ├── constants.js      # N8N_WEBHOOK_URL
│   └── api-service.js   # checkLetterStatus
└── tests/
    └── cover-letter.test.js  # Tests verify behavior

n8n-workflows/
└── local_5678_osio_a/personal/
    └── 3.Job Application Writer.workflow.ts
```

**Structure Decision**: Browser extension project. Changes to existing files only. No new modules needed. The n8n workflow file was fixed in-place.

## Complexity Tracking

No violations - no new code required, reusing existing implementation.

## Implementation Summary

### Configuration Fixes Applied

| Fix | File | Issue | Solution |
|-----|------|------|---------|
| 1 | 3.Job Application Writer.workflow.ts | Webhook not responding to POST | Added `httpMethod: 'POST'` |
| 2 | 3.Job Application Writer.workflow.ts | job_offers_id not captured | Changed `query.job_offers_id` to `body.job_offers_id` |
| 3 | 3.Job Application Writer.workflow.ts | dbTime.getTime error | Removed unmapped `created_at` dateTime field from schema |

### Verification

- Tests: 23/23 passed
- Webhook: Returns 200 "Workflow was started"
- Database: New job_applications created (IDs 39, 40)
- All tasks marked complete in tasks.md